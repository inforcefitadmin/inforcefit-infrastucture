---
- name: Setup InforceFit VPS Infrastructure
  hosts: webservers
  become: true

  vars_files:
    - ../vars/main.yml

  roles:
    - role: ../roles/base
    - role: ../roles/security
    - role: ../roles/nodejs
    - role: ../roles/nginx
    - role: ../roles/monitoring